# PostgreSQL コードコメント規約 (Claude Code 向け)

このリポジトリで C コードを書く／変更するときのコメント規約。
PostgreSQL は「コメントが多い」コードベースだが（`src/backend/*.c` の全行のうち
約 28% がコメント行）、その中身は **コードを読めば分かることの言い換えではなく、
コードからは読み取れない理由・前提・不変条件** に限られている。
Claude が生成しがちな「1 行ごとの解説」「引数一覧の羅列」「変更履歴の書き置き」は、
量が多いだけで PostgreSQL のコメントとは種類が違う。以下はその差を埋めるためのルール。

判断に迷ったら、変更対象のファイルの周囲 200 行を読んで同じ書き方に合わせること
(`doc/src/sgml/sources.sgml`: "make the new code look like the existing code around it")。

---

## 1. 大原則

1. **What ではなく Why を書く。** コードが「何をしているか」は読めば分かる。
   書くべきは「なぜそうしているか」「なぜ他の方法ではないか」「壊れる条件は何か」。
2. **コメントを消してもコードの意味が変わらないなら、そのコメントは消す。**
3. **名前で説明できることはコメントにしない。** コメントが必要なら、まず改名を検討する。

---

## 2. 書式 (`doc/src/sgml/sources.sgml` の Formatting 節)

- コメントは**英語**で書く。
- `//` は使わない (`src/backend` 全体で使用例 0 件)。`pgindent` が `/* */` に書き換える。
- 複数行は次の形。`*` の縦位置を揃える。

  ```c
  /*
   * comment text begins here
   * and continues here
   */
  ```

- ASCII 図・箇条書き・番号付きリストなど改行を保持したいブロックだけ `/*----------` を使う
  (例: `src/backend/access/nbtree/nbtinsert.c:924`)。それ以外では使わない。
- 1 行は 78 桁程度で折り返す (既存コメント行の p99 = 78 桁)。
- Doxygen / JSDoc 風のタグ (`@param`, `@return`, `@brief`) は**使わない**
  (`src/backend` に 1 件も存在しない)。
- 行末コメントは**宣言・構造体メンバ・enum に限る**。実行文の行末には付けず、
  ブロックの上に置く（関数内の単独 1 行コメント約 33,000 件に対し、
  実行文の行末コメントは約 2,000 件）。

  ```c
  typedef struct AggState
  {
      ScanState   ss;             /* its first field is NodeTag */
      List       *aggs;           /* all Aggref nodes in targetlist & quals */
      int         numaggs;        /* length of list (could be zero!) */
  ```
  (`src/include/nodes/execnodes.h`)

---

## 3. 書いてはいけないコメント

### 3.1 コードの言い換え

処理を日本語訳／英語訳しただけのコメントは書かない。

```c
/* 悪い例 */
    /* Initialize the counter to zero */
    nitems = 0;

    /* Loop over all items on the page */
    for (off = FirstOffsetNumber; off <= maxoff; off++)
    {
        /* Get the item id */
        ItemId      itemid = PageGetItemId(page, off);

        /* Skip the item if it is not used */
        if (!ItemIdIsUsed(itemid))
            continue;
```

```c
/* 良い例: 自明な行にはコメントを付けない */
    nitems = 0;
    for (off = FirstOffsetNumber; off <= maxoff; off++)
    {
        ItemId      itemid = PageGetItemId(page, off);

        if (!ItemIdIsUsed(itemid))
            continue;
```

既存コードでも、自明な初期化の並びには**まとめて 1 行**しか付けない。
`ExecInitAgg()` (`src/backend/executor/nodeAgg.c`) は 20 行以上の
フィールド初期化に `/* create state structure */` の 1 行だけ。

### 3.2 引数・戻り値の機械的な列挙

```c
/* 悪い例 */
/*
 * _bt_findinsertloc
 *
 * Parameters:
 *   rel           - the index relation
 *   insertstate   - insertion state
 *   checkingunique - whether we are checking uniqueness
 *   stack         - the search stack
 *   heapRel       - the heap relation
 *
 * Returns: the offset number at which to insert.
 */
```

シグネチャを読めば分かることは書かない。書くのは**引数から読み取れない契約**
(呼び出し側が保持しているべきロック、メモリの所有権、エラー時の挙動、
戻り値が `Invalid*` になる条件など)。
`src/backend` には "Caller must ..." が 264 箇所ある一方、`@param` は 0 件。

```c
/* 良い例: src/backend/access/nbtree/nbtpage.c:813 */
/*
 *	_bt_getbuf() -- Get an existing block in a buffer, for read or write.
 *
 *		The general rule in nbtree is that it's never okay to access a
 *		page without holding both a buffer pin and a buffer lock on
 *		the page's buffer.
 *		...
 *		Note: raw LockBuffer() calls are disallowed in nbtree; all
 *		buffer lock requests need to go through wrapper functions such
 *		as _bt_lockbuf().
 */
```

引数の説明そのものが必要になるのは、名前から用途が読み取れないときだけ。
その場合も箇条書きの表ではなく散文で書く。

### 3.3 変更履歴・作業ログ

差分の説明はコミットメッセージに書く。コードには残さない。

```c
/* 悪い例 */
    /* NEW: added parallel worker support (this was serial before) */
    /* Changed from palloc() to palloc0() to fix the reported bug */
    /* Renamed from old_function_name() */
    /* Added per review feedback */
```

例外は、**現在のコードの形を説明するために歴史が必要な場合だけ**で、
その場合もファイル冒頭のヘッダブロックに置く
(`src/backend/utils/mmgr/aset.c:27` "Performance improvement from Tom Lane, 8/99: ..."
— なぜ大きなチャンクを free() に返すのかを説明している)。

### 3.4 手順の実況中継

```c
/* 悪い例 */
    /* Step 1: acquire the lock */
    /* Step 2: read the page */
    /* Step 3: release the lock */
```

コードの構造がそのまま手順を表しているなら、番号付けは冗長。
番号付きの手順を書いてよいのは、**コードの並びからは読み取れないアルゴリズム全体像**を
先に示す必要があるときだけ (例: `nbtinsert.c` の "Keep scanning right until we (a)... (b)... (c)...")。

### 3.5 メタコメント・AI 由来の言い回し

```c
/* 悪い例 */
    /* Helper function to process the tuple */
    /* This function will iterate over all the entries and return the result */
    /* Implemented as requested */
    /* Generated by an AI assistant */
```

`Helper function to` / `This function will` のような枕詞は付けない。
主語を省いた現在形の平叙文で書く。既存コードでは主語に `we` を使うのが普通
(`src/backend` に約 11,700 件)。

### 3.6 Assert / 明らかなガードへの注釈

`Assert()` はそれ自体がドキュメント。「何を検査しているか」を訳したコメントは付けない。
`_bt_findinsertloc()` は連続する 5 個の `Assert()` にコメントを 1 つも付けていない。
付けるなら「**なぜ**その条件が成り立つと言えるのか」だけ。

### 3.7 変更していないコードへのコメント追加

パッチのついでに周囲の既存コードへコメントを足さない。レビュー対象の差分が
本質的な変更で埋まらなくなる。コメント整備は独立した変更として行う。

---

## 4. 書くべきコメント

以下は「冗長」ではない。削らないこと。

- **関数ヘッダ**: 定義側 (`.c`) に置く。ヘッダファイルのプロトタイプには付けず、
  関連プロトタイプ群の上に `/* prototypes for internal functions in nbtree.c */`
  のようなグループ見出しを 1 つ置く (`src/include/access/nbtree.h:1184`)。
- **呼び出し側との契約**: 必要なロック／ピン、呼び出し順序、メモリコンテキストの前提。
  例: `heapam.c:6903` "NB: Caller must maintain ... trackers"。
- **不変条件・オンディスク表現**: `numeric.c:108` 付近の `NumericChoice` の説明のように、
  ビットレイアウトや「パック形式では前後のゼロ桁が落ちている」といった規約。
- **なぜ他の書き方をしないか**:
  `numeric.c:182` "Instead of testing against each value, we can just look at the
  high bit, for a slight efficiency gain."
- **一見おかしく見えるコードの根拠**: `nbtree.c:1358`
  `/* call vacuum_delay_point while not holding any buffer lock */`
  のように、その位置でなければならない理由。
- **既知の問題**: `XXX` を使う (`src/backend` に 641 件)。`TODO` (40 件) /
  `FIXME` (20 件) は稀なので新規に増やさない。未完成のまま `TODO` を残してコミットしない。
  例: `costsize.c:1328` `/* XXX currently we assume TID quals are a subset of qpquals */`

短い 1 行コメントで十分なことが多い。既存コードの典型:

```c
    /* bootstrap mode in particular breaks this rule */
    /* if the relation doesn't have a TOAST table, we are good */
    /* may return InvalidBlockNumber if there are no more blocks */
```
(`src/backend/access/heap/heapam.c`)

---

## 5. コードを変更するとき

- コードを変えたら、**その上のコメントも直す**。古い挙動を説明したままのコメントを残さない。
- コメントを消す変更をするときは、それが本当に不要か（3 章の型に当てはまるか）を確認する。
- 動作を変えた関数のヘッダコメントに書かれた契約 (ロック要件、戻り値の意味) が
  まだ正しいか確認する。

---

## 6. コミット前のセルフチェック

1. 直下のコードを読めば分かることを書いているコメントはないか。
2. `@param` / `Parameters:` / `Returns:` 形式の羅列を書いていないか。
3. 「追加した」「変更した」「修正した」と書いたコメントはないか（コミットメッセージへ移す）。
4. `Helper function to` / `This function will` / `Step 1:` で始まるコメントはないか。
5. 変更していない行にコメントを足していないか (`git diff` で確認)。
6. `//` を使っていないか。1 行 78 桁を大きく超えていないか。
7. コメントとコードの内容が食い違っていないか。
