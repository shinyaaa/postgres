# pg_subtrans ハッシュ化 — リスクの根本的解決策（設計編）

本書は `feasibility_subtrans_dshash_ja.md` の §5「リスクと未解決課題」への回答である。
単なる緩和策（大きめの上限・チューニング指針）ではなく、**リスクの発生源そのものを
設計から除去する**ことを狙う。

---

## 0. 設計原則 — コミュニティに受け入れられるための必要条件

過去の類似提案（multixact 周りの障害の教訓、リカバリコンフリクトへの忌避感、
コアパスへの新規 ERROR 導入への強い抵抗）を踏まえると、受け入れの必要条件は
次の 4 原則に集約できる。

1. **新しい失敗モードを作らない**。SAVEPOINT が「メモリ都合で ERROR になる」、
   リプレイが「メモリ都合で止まる/PANIC する」、スタンバイクエリが「新種の
   コンフリクトでキャンセルされる」— いずれも導入した瞬間に提案が死ぬ。
2. **メモリ上限は起動時に確定し、実行時に成長しない**（真の静的確保）。
   DSM の弾力成長で OS OOM に賭ける設計は、OOM killer が PANIC より悪い結末を
   招くため不可。
3. **最悪ケースの逃がし先は今日と同じ「ディスク」**。ただし SLRU より単純で、
   ゼロ領域を書かない形式にする。「厳密に今日より悪くならない」ことを
   証明可能な形で主張の中心に据える。
4. **データの性質を最大限利用する**。汎用構造（dshash）をそのまま使うのではなく、
   subtrans データ固有の性質に特化した専用構造にする。

---

## 1. 鍵になる観察 — subtrans データの 3 つの性質

解決策の土台として、コードで確認済みの 3 性質を明示する。
**この 3 性質こそが、汎用 dshash では解けなかった問題を解く鍵である。**

### 性質 1: write-once（エントリは不変）
親は一度書かれたら決して変わらない。`SubTransSetParent` は同値の再書き込みのみ許し、
有効な親の上書きを Assert で禁止している（subtrans.c:110-120）。個別削除も存在せず、
破棄は horizon 以下の一括切り捨てだけ。
→ **更新が無いので並行制御が根本的に楽になる。** lock-free 読み取り（§4）と
不変スピルファイル（§2）が可能になる。

### 性質 2: キーが時間軸（xid は単調、破棄は一括）
挿入はほぼ常に「最新の xid 帯」に起き、破棄は「horizon より古い帯」を丸ごと捨てる。
→ **世代（epoch）構造と完全に整合する。** 古い世代を丸ごとスピル/破棄でき、
ハッシュを seq scan して古いキーを探す必要がない。

### 性質 3: 参照の時間局所性（ホットな参照はすべて最近の xid）
- `TransactionIdDidCommit` の SUB_COMMITTED 経由の親参照は、コミット処理が
  複数 clog ページにまたがる**短い過渡窓**でしか起きない。コミット完了後は
  subxid 自身が clog 上 COMMITTED になるため親参照は不要
  （clog.c:145-190 の 3 段階更新プロトコルで確認。クラッシュで SUB_COMMITTED が
  残留するケースは `xid < TransactionXmin` → 「親はクラッシュ」判定で処理済み、
  transam.c:144-157）。
- `TransactionIdIsInProgress` / `XactLockTableWait` / 可視性判定の大半も
  実行中トランザクション近傍の xid。
- **古い xid への参照**は「suboverflowed な老スナップショットの可視性判定」と
  「SSI の conflict-out」だけであり、低頻度。しかも**その領域は今日でも SLRU の
  バッファミス = ディスク I/O になっている**。
→ **「最近 = メモリ、古い = ディスク」の 2 層化は、参照パターンと一致する。**

---

## 2. 【5.1 の根本解決】世代別インメモリ表 + 不変ソート済みスピル

### 2.1 提案する構造（subtrans 専用構造。仮称 `subtranscache.c`）

```
静的共有メモリ（起動時確定、GUC subtrans_mem / 既定は autotune）
┌─────────────────────────────────────────────┐
│ epoch テーブルの固定プール                      │
│  [epoch k-2: sealed] [epoch k-1: sealed] [epoch k: open] │
│  各 epoch = 固定容量の追記専用ハッシュ            │
│    エントリ: (subxid 4B, topxid 4B) + チェーン 4B ≈ 16B/entry │
│ straggler テーブル（小、常設）                   │
└─────────────────────────────────────────────┘
            │ プール枯渇時: 最古 epoch を丸ごとスピル
            ▼
ディスク（$PGDATA/pg_subtrans/、非永続、起動時に全削除）
  不変ソート済みラン: (xid, topxid) ペアの列 + ブロック索引
```

- **挿入**: open epoch へ追記。epoch が満杯になったら seal して新 epoch を開く
  （O(1)、ロックはパーティション局所）。
- **スピル**: プールが枯渇したら最古の sealed epoch を xid ソート済みの
  不変ファイル（ラン）として書き出し、メモリを即回収。書き出しは原則
  checkpointer/bgwriter が先行して行い（プール使用率 75% 超で着手）、
  フォアグラウンドは最後の砦としてのみ実施（= 今日の SLRU バッファ追い出しで
  バックエンドがページを書くのと同じ位置づけ・同じコスト構造）。
- **参照**: xid で経路を決める。epoch 境界（少数、二分探索）→ in-memory find。
  境界より古ければ straggler → スピルラン（メモリ上のブロック索引
  [ブロック先頭 xid の配列、エントリ 2048 個あたり 4B ≈ 実質ゼロコスト] で
  対象 8KB ブロックを特定し **pread 1 回**）。
- **切り捨て**（TruncateSUBTRANS 相当）: horizon より古い epoch とランを
  **丸ごと破棄**（メモリは O(1) で回収、ファイルは unlink。今日の
  `SimpleLruTruncate` と同じ）。
- **起動**: 全ファイル削除 + 空プール初期化（今日の `StartupSUBTRANS` の
  ゼロクリアと同じ理屈。EnableHotStandby ガード付きの微妙な順序制約
  [xlog.c:8361-8364] も消える）。

### 2.2 なぜこれが「根本」解決なのか

§5.1 の問題は「ハッシュ化でディスクスピルを失うため、メモリが非有界になり、
溢れたときの挙動（ERROR / リプレイ停止）が受け入れ不能」だった。本設計では:

| 論点 | 回答 |
|---|---|
| メモリ上限 | 起動時に確定した固定プール。**実行時成長なし**（原則 2） |
| プライマリで溢れたら | ERROR ではなく**スピル**。SAVEPOINT は失敗しない（原則 1）。最悪でもフォアグラウンドのファイル書き込み待ち = 今日の SLRU 追い出しストールと同型 |
| スタンバイで溢れたら | 同じくスピル。**リプレイは失敗しない**。startup プロセスによるファイル書き込みは今日も起きている（`RecordKnownAssignedTransactionIds` → `ExtendSUBTRANS` → SLRU ページ追い出し）ので、新規性なし |
| リカバリコンフリクト新種 | **不要**（スピルが吸収するため。導入しない） |
| 通常ワークロード | スピル未発生 = **純インメモリ動作**。Andres 案の性能効果（suboverflow 崖の消滅、書き込み分散、ゼロページ I/O 消滅）をそのまま実現 |
| 病的ワークロード（長時間スナップショット + plpgsql 例外ブロック洪水） | 古い世代がディスクへ。参照するのは老スナップショットだけで、**それは今日でもディスク読みだった**。しかもスピルは実在エントリのみを密に持つため、今日の「ほぼ全ゼロの SLRU ページを読む」より I/O 効率は良い |

「今日と比べて、失敗モードは増えず、メモリは有界になり、通常時は速くなり、
最悪時の逃がし先は同じディスク」— これが証明可能な形で主張の中心になる。

### 2.3 スピル形式が SLRU 再導入より圧倒的に単純である理由

「結局ディスクを使うなら SLRU 廃止の意味がない」という反論への回答。
性質 1（write-once）と性質 2（一括破棄）により、スピルは SLRU が持つ複雑さの
ほぼ全てを捨てられる:

- **不変ファイル**: 追記も更新もなし。書いたら読み専用、消すときは unlink。
  → バッファ管理・ダーティ追跡・ページ置換・バンクロックが全部不要。
- **fsync 不要**: 非永続（クラッシュしたら全削除）。sync ハンドラなし。
- **wraparound 処理不要**: epoch 境界メタデータが FullTransactionId を持てばよい。
  SLRU のページ番号写像・過去/未来判定の複雑さが消える。64-bit XID 化とも整合。
- **密な実データのみ**: ゼロ領域を書かない。1 ランのサイズ = そこに実在した
  subxid 数 × 8B。
- 実装規模の見積もり: ラン書き出し + ブロック索引 + pread 読み。slru.c（約 2,000 行の
  汎用機構）に対し、数百行のオーダーで収まる見込み。

### 2.4 厄介ケースの処理（査読で突かれる箇所を先回りで潰す）

**(a) スタンバイの「古い xid への挿入」（straggler）**
`XLOG_XACT_ASSIGNMENT` が運ぶ subxid は「前回報告以降の蓄積分」であり、
トランザクションが subxid 割り当ての合間に長時間アイドルだった場合、
xid 距離では任意に古くなり得る（xact.c:736-790 で確認。unreportedXids は
64 個貯まるまで報告されない）。つまり**スピル済み範囲への挿入は起き得る**。
- 対策: 常設の小さな **straggler テーブル**を設け、スピル済み範囲への挿入は
  そこへ入れる。参照経路は epoch → straggler → スピルラン。
- 有界性の論証: straggler に入るのは「対応する epoch がスピルされるほど古いのに、
  今になって WAL に現れた」subxid のみ。1 レコードあたり最大 64 個で、
  該当するのは長時間アイドルだったトランザクションの残滓だけなので流量は小さい。
  万一 straggler が埋まったら、次のスピル時に一緒にランへマージすればよい
  （ランは epoch 単位でなくても「xid 範囲 + ソート済み」でありさえすれば
  参照経路は変わらない）。

**(b) 2PC リカバリ / プロモーション時の再書き込み**
`ProcessTwoPhaseBuffer`（twophase.c:2301）は「既に親が設定済みかもしれない」
xid に冪等な再設定を行う。プロモーション時（長いリカバリの後 = スピルが存在し得る）
にこれがスピル済み範囲を対象にする可能性がある。
- 対策: 書き込み側でスピルランを検索し、存在すれば no-op（値は必ず一致する。
  write-once 性質による）、不在なら straggler へ挿入。プロモーション時のみの
  低頻度パスなので pread コストは無視できる。

**(c) 挿入の可視性順序**
「xid が PGPROC 以外の共有領域に現れる前に subtrans エントリを作る」という
不変条件（xact.c:703-706）は、epoch 内挿入をパーティションロック（§4 導入後は
release store）で公開してから `SubTransSetParent` を返すことで維持する。
今日のバンクロックと同じ強さの順序保証。

**(d) スピル境界の一貫性**
境界（どの xid からメモリか）は共有の単一値（atomic な FullTransactionId）。
スピル完了 → 境界前進 → epoch メモリ解放、の順で行えば、読者は常に
「境界より新しければメモリに必ずある」を信頼できる。境界後退は起きない
（straggler が古い側の挿入を吸収するため）。

### 2.5 検討して却下した代替案（提案時に明記すべき比較）

- **案 B: 純インメモリ + 上限超過時はプライマリ ERROR / スタンバイは
  リカバリコンフリクト**
  - SAVEPOINT が容量都合で失敗する新 ERROR は、ワークロード依存で予測不能。
    コアの信頼性後退としてまず受け入れられない（原則 1 違反）。
  - リカバリコンフリクト新種はユーザ可視の後退。ホットスタンバイの体験を悪化させる。
  - スピル付き設計はこの案の上位互換（同じメモリで、失敗の代わりに退避）。
- **案 C: DSM による無制限の弾力成長**
  - 上限を OS に転嫁するだけ。OOM killer による postmaster/backend の死は
    ERROR より悪い（原則 2 違反）。
- **案 D: 範囲圧縮（連続 xid 区間 → top の区間表現）**
  - 並行トランザクションの xid 割り当てが交錯すると区間が細片化し、最悪ケースで
    ハッシュと同等以下。**保証がない最適化**であり、上限問題の解決にならない。
    （将来の付加的最適化としては有望。epoch 内表現を後から差し替え可能な
    設計にしておく）
- **案 E: 共有メモリ radix tree（radixtree.h, RT_SHMEM）の流用**
  - ツリー全体で単一 LWLock（radixtree.h:691, 1901-1915）であり、
    `SubTransSetParent` の書き込み並行性要件を満たさない。却下。
    （キーの時間軸性・一括破棄との相性は良いので、将来ロック分割されたら再考の余地）

---

## 3. 【5.2 の根本解決】リサイズという操作を構造から消す

§5.2 の問題は「dshash の倍々リサイズが全 128 パーティションロックを排他取得して
全要素を再ハッシュする」ことだった。緩和（プリサイズ）ではなく根絶する:

- 原則 2 によりメモリ上限は起動時に確定している。したがって **epoch テーブルは
  すべて固定容量でプリアロケートできる**。「テーブルが満杯になる」ことは
  異常でも成長トリガでもなく、「新しい epoch を開く」という O(1) の定常動作になる。
- つまり本設計には**リサイズが存在しない**。全域ロック取得も再ハッシュも
  構造上発生し得ない。レイテンシスパイクの源が消える。
- この帰結として、汎用 dshash をそのまま使う案は放棄し、専用構造にする。
  dshash が汎用性のために抱える機能（動的成長、seq scan 中のリサイズ排除、
  DSA 上のバケット再確保）は本用途では全て不要であり、pgstat の
  「in-place DSA + dshash」先行事例は**「静的共有メモリ上の並行ハッシュは
  実績がある」という土台の証明**として引用しつつ、実装は特化する。
  （これは radix tree が TID 用に `radixtree.h` として特化実装された経緯とも
  整合的で、コミュニティの流儀に合う）

---

## 4. 【5.3 の根本解決】write-once 性を使った lock-free 読み取り

§5.3 の問題は「Stage 2 で可視性ホットパスがロックフリー SIMD 探索から
LWLock 付きハッシュ参照に変わり後退し得る。汎用 lock-free ハッシュは難物」だった。

**鍵は §1 の性質 1〜2 により、必要なのは汎用 lock-free ハッシュではない**という点:

- エントリは不変（更新なし・個別削除なし）。
- epoch 内は head 追記のみ。
- 破棄は epoch 丸ごと、かつ horizon より古い = 「読者がもう参照しないと
  証明済み」の領域のみ。

この 3 条件は、RCU/エポックリクレイム型の読み取りが教科書通りに適用できる
最も簡単なクラスである:

- **読者**: ロックを一切取らない。バケットチェーンを acquire load で辿るだけ
  （挿入は「エントリを完全に書く → release store でチェーン head に繋ぐ」ので、
  読者は常に完全なエントリしか見えない）。
- **破棄側**: epoch を解放する前に grace period を置く。読者は per-backend の
  「参照中」表示（atomic フラグまたは世代番号。ProcArray の既存手法と同型）を
  立ててから読むので、破棄側は全バックエンドの表示が消えるのを待つ。
  読者の滞在は数十命令なので待ちは実質ゼロ。しかも破棄対象は horizon より
  古い epoch のみで、そもそも正当な読者がいない（subtrans.c:138 の
  `Assert(xid >= TransactionXmin)` がその契約を既に表現している）。
- **挿入側**: パーティション局所のロック（または CAS）で直列化。挿入同士の
  競合だけを制御すればよく、読者を一切ブロックしない。

これにより Stage 2 の前提条件が満たせる:
- `XidInMVCCSnapshot` のホットパスは「ロックなし・アロケーションなしの
  ハッシュ参照 1 回（+ 親チェーン、Stage 1.5 の top 直接格納を併用すれば 1 ホップ）」
  になり、`pg_lfind32` による subxip 走査との差は定数レベルに縮む。
- さらに per-backend の小型キャッシュ（直近に解決した subxid→top 対の数エントリ。
  既存の `cachedXidIsNotInProgress`[procarray.c:1423-1427] の一般化）を挟めば、
  ホットタプル集中時の参照はほぼローカルで吸収できる。

それでも Stage 2 は**実測ゲート付き**とする（読み取り集中ベンチで parity を
確認できるまで subxip 併存構成に留める）。Stage 1 + lock-free 読み取りだけでも、
suboverflow 時の可視性判定が「LWLock + SLRU ページ探索（+ ディスク）」から
「lock-free メモリ参照」になるため、単独で大きな改善である。

---

## 5. 長期解（CSN）との関係 — 「将来と衝突しない」ことの明示

老スナップショットが horizon を留める問題の究極の根治は CSN ベースの
スナップショット（xip/subxip/suboverflowed という概念自体の廃止）だが、
それは別プロジェクト規模であり、本提案の前提にはしない。

重要なのは方向の整合性である: 本設計の subxid→top マップは CSN 化後も
`XactLockTableWait` や SSI のために必要であり続けるため、**本提案は CSN への
布石になりこそすれ、障害にはならない**。提案文にこの一節を入れることで
「どうせ CSN でやり直しになる」という反論を先に無効化する。

---

## 6. 提案パッケージング（hackers への出し方）

1. **実装前に design doc を投稿し、§2 の設計（特にスピルの是非と straggler の扱い）
   で合意を取る**。5.1 相当の議論は必ず起きるので、先回りして §2.5 の却下理由まで
   含めて提示する。
2. パッチ分割:
   - 0001: subtrans API の呼び出し側整理（`ExtendSUBTRANS`/`CheckPointSUBTRANS` の
     no-op 化を許す形へのリファクタ。単独でコミット可能な準備パッチ）
   - 0002: `subtranscache.c`（epoch プール + straggler + スピル）実装、
     SLRU 実装と GUC で切り替え可能な形で導入（比較検証のため）
   - 0003: SLRU 実装の削除、basebackup/pg_rewind/pg_stat_slru/文書の追随、
     新監視ビュー（エントリ数・スピル量・straggler 使用量）
   - 0004: lock-free 読み取り
   - 0005: Stage 2（subxip/PGPROC キャッシュ/suboverflowed の廃止）— 独立提案
3. テスト計画:
   - fault injection: スピル強制（極小プール）、straggler 強制（アイドル挟み
     ワークロード + スタンバイ）、プロモーション中の 2PC 再構築、
     スピル読み経路の並行性
   - ベンチ: (a) subxid 非使用の中立性、(b) savepoint 洪水 + 長時間スナップショット
     （改善の主戦場）、(c) スタンバイのリプレイ速度、(d) スピル発生時の劣化が
     「今日の SLRU ミス」と同等以下であること
4. 主張の一行要約（コミットメッセージ / 提案の冒頭に置く）:
   「**メモリ上限は固定、失敗モードは不変、通常時は純インメモリで高速、
   最悪時は今日と同じディスクへ — ただし今日より単純で密な形式で。**」

---

## 7. 残リスク（誠実な開示）

根本解決後もゼロにならないものを列挙する（提案時に自ら開示すべき項目）:

- **スピル発生中のフォアグラウンド書き込み待ち**: 先行スピルが追いつかない
  瞬間的バーストでは、挿入側がラン書き出しを待つ。今日の SLRU 追い出しストールと
  同型・同程度だが、「新実装で顕在化した」と受け取られないよう、
  ベンチで today との比較を示す必要がある。
- **メモリ会計の変化**: `subtransaction_buffers`（ページキャッシュの大きさ）から
  `subtrans_mem`（ライブエントリの器）へ意味が変わる。移行ガイドと
  autotune の妥当な既定値（shared_buffers 比例 + 上限）の設計が必要。
- **実装の新規性そのもの**: SLRU は枯れている。専用構造 + スピル + lock-free は
  新規コードであり、レビュー負荷が高い。0002 で新旧切り替え可能にして
  比較検証期間を設けるのはこのため。
