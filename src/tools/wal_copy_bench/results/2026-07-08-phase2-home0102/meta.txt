Phase 2 decision run on real storage (home0102), 2026-07-08

Host: home0102 (192.168.1.3), 11th Gen Intel Core i5-1135G7 @ 2.40GHz, 8 CPUs,
      15GB RAM, Rocky Linux 10 (kernel 6.12.0-124.8.1.el10_1)
Storage: WD Blue SN570 500GB NVMe (xfs on LVM; /home).  An idle long-lived
      postgres dev cluster was running on the host (same as during the
      2026-07-08 perf run); load average ~0.1 before the runs.
Build: 20devel @ 42435e30f57 (Phase 2 prototype cherry-picked), gcc 14.3.1
      ./configure --with-zstd --with-lz4 --without-icu
                  CFLAGS='-O2 -fno-omit-frame-pointer -g'
      libzstd 1.5.5, lz4 1.9.4 (lz4-devel installed for this run; the
      2026-07-08 perf build had no --with-lz4)
Harness: run_bench.sh with SCALE_NARROW=10000000 SCALE_WIDE=600000 MAX_JOBS=4
      FSYNC=on, BENCH_ROOT on the same NVMe (/home/shinya/pgsql/copy-wal-perf/
      work/bench).  Includes the new 'rand' width (base64 of /dev/urandom,
      ~1KB rows; compresses to ~75% at best = entropy floor of base64, no
      LZ matches).  Fixed server settings as usual: shared_buffers=2GB,
      wal_buffers=16MB unless noted, max_wal_size=16GB,
      checkpoint_timeout=30min, autovacuum=off, track_wal_io_timing=on.

Three runs, in order:
  summary.csv rows 1-26  = run 1, the built-in default matrix (single shot)
  summary.csv rows 27-37 = run 2, CASES_FILE extras: img+lz4 cases and the
                           rand width cases (single shot)
  summary-repeat.csv     = run 3, the 20 decisive cases x 3 repetitions,
                           interleaved (_r1 block, _r2 block, _r3 block) to
                           spread environmental drift across cases

wait_events/<case>.txt = per-case wait-event histograms of the loading
backends, summed over the 3 repetitions of run 3 (50ms sampling).

Repeatability (run 3): min/max spread of elapsed within a case is ~1% for
almost all cases; exceptions called out in report.md.  Single-shot run-1/2
numbers agree with run-3 medians except wide_replica_j1 (1.415s single shot
vs 1.263s median; use the median).

Data compressibility check (zstd -1 on whole load files):
  wide payload compresses to ~1% (highly repetitive synthetic text),
  rand payload to ~75% (entropy floor).  Real-world data sits in between;
  the two widths bracket it.
